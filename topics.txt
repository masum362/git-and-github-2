git add file_name
git commit -m 'Message'

git status

git diff
git diff --staged

git log
git log --online

git restore --staged index.html //back index.html from staged 
git reset --soft HEAD~1 //back last commit to stage
git revert

git remote add origin
git remote set-url <remote-name> <url>


git branch
git checkout
git checkout -b feature/login-form

github to local pc clone / zip download
git pull

merge conflict
git merge

fork
pull request 

github collaboration
